import { describe, it } from 'vitest';

describe('Supply Position swapping on Aave V4', () => {
  describe('Given a user with a supply position able to swap', () => {
    describe('When the user swaps part of the position for another asset with a market order', () => {
      it.todo(
        'Then the user should be able to swap and the position should be updated',
      );
    });

    describe('When the user swaps part of the position for another asset with a limit order', () => {
      it.todo(
        'Then the user should be able to swap and the position should be updated',
      );
    });
  });
});
