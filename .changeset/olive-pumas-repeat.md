---
"@aave/graphql": minor
"@aave/client": minor
"@aave/react": minor
---

**feat:** expose `Chain.nativeAsset`, gate the wrapped-native display transform on a real wrapper, and add `collapseNativeErc20Balances` so a chain whose native token is itself an ERC20 is neither dressed in native token info nor counted twice
