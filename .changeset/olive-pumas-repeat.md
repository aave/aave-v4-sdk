---
"@aave/graphql": minor
"@aave/client": minor
"@aave/react": minor
---

**feat:** expose `Chain.nativeAsset` and gate the wrapped-native display transform on a real wrapper, so a chain whose native token is itself an ERC20 is never dressed in native token info
