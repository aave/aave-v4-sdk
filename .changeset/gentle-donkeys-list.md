---
"@aave/cli": minor
---

**feat:** add CLI commands to list available and user-approved position managers

Adds `aave spokes position-managers` for inspecting managers configured on a spoke, and `aave user position-managers` for checking which managers a user has approved.
