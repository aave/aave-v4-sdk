---
"@aave/graphql": patch
"@aave/client": patch
"@aave/react": patch
"@aave/core": patch
---

**feat:** add `SwapOperation` union to `SwapStatus` entries. Support `InsufficientLiquidityError`. Add `orderClass` and `kind` to `SwapStatus` and ActivityItem` union entries.
