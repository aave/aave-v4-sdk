---
"@aave/graphql": minor
"@aave/client": minor
"@aave/react": minor
---

**feat:** add the `liquidationsSummary` action and `useLiquidationsSummary` hook with per-bucket liquidation `items`, the `reserveId` query on `activities`, the `BtcCorrelated`, `ProtocolToken` and `InfrastructureToken` values of `TokenCategory`, and five config-change activities with before and after values: `UpdatedLiquidationConfigActivity`, `UpdatedPriceSourceActivity`, `UpdatedReserveConfigActivity`, `UpdatedSpokeConfigActivity` and `UpdatedPositionManagerActivity`, requested through their `ActivityType` values
