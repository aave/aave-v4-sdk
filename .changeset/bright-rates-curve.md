---
"@aave/graphql": minor
"@aave/client": minor
"@aave/react": minor
---

**feat:** add `hubAssetInterestRateModel` query, action, and hooks for fetching interest rate curve data; add new fields to `HubAssetSettings` (`optimalUtilizationRate`, `baseBorrowRate`, `slopeBelowOptimal`, `slopeAboveOptimal`) and `ReserveSettings` (`borrowCap`, `supplyCap`)
