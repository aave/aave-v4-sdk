---
"@aave/client": minor
"@aave/react": minor
---

**feat:** add `resolveNativeAssets` option to `AaveClient` to automatically resolve wrapped native tokens (e.g. WETH → ETH) in all protocol query results
